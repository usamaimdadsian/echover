node_modules/
dist/
src-tauri/target/
src-tauri/Cargo.lock
.DS_Store
*.db
*.db-shm
*.db-wal
*.log
target/
.codex


# Anchor to the repo root so we don't clobber `src/library/` (the scanner
# module) or `src/data/` etc. of similar names.
/data/
/library/

# Visual-spec bundle (1.6 MB exported HTML). Kept locally as the design
# reference but never tracked, and purged from history.
/design/
